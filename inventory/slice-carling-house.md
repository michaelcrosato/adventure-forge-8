# Slice 129 uniqueness claim — Carling House

Region id: `carling_house`. Hamlet pocket. Not a reskin of knighthead score-box-lash, joist notch-trimmer, or transom spike-stern.

## Mechanic

Notch, lodge, spike. The carling is notched, the ledge is lodged, and the carling is spiked. Not notch_the_trimmer, not lodge_the_end, not spike_the_stern.

## Inhabitants

- Notch (yard)
- Ledge (ledge)
- Spike (spike)

## Locations

`carl.path`, `carl.yard`, `carl.notch`, `carl.ledge`, `carl.spike`. Linked from `ashfen.causeway` and `kni.path`. Loom owns “Go to the beam”; stair owns “Go to the housing”; mast owns “Go to the range”.

## Outcome

`carling_spiked` — flag `carling_spiked`. Witness: `traces/marsh_carling_spiked.json`.

## Sheet / deed gates

Same scene `carl.yard`: marshborn/hunt `know_the_notch`; letters `read_the_carling_list`.

## Cross-effect

Lashed knight (`knight_lashed`) unlocks `lash_the_notch` at the notch bench. Proven by `cross_kni_carl` vs `cross_plain_carl`.
