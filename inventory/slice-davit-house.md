# Slice 124 uniqueness claim — Davit House

Region id: `davit_house`. Hamlet pocket. Not a reskin of gunwale snape-hance-dump, hinge ship-gudgeon, or windlass ship-bars.

## Mechanic

Sweep, band, ship. The davit is swept, the crane is banded, and the davit is shipped. Not snape_the_covering, not ship_the_gudgeon, not ship_the_bars.

## Inhabitants

- Sweep (yard)
- Crane (crane)
- Ship (ship)

## Locations

`dav.path`, `dav.yard`, `dav.sweep`, `dav.crane`, `dav.ship`. Linked from `ashfen.causeway` and `wale.path`. Gilt has actor Band; baluster owns “Go to the neck”. Dairy owns “Go to the dairy”.

## Outcome

`davit_shipped` — flag `davit_shipped`. Witness: `traces/marsh_davit_shipped.json`.

## Sheet / deed gates

Same scene `dav.yard`: marshborn/hunt `know_the_sweep`; letters `read_the_davit_list`.

## Cross-effect

Dumped bolt (`bolt_dumped`) unlocks `dump_the_sweep` at the sweep bench. Proven by `cross_wale_dav` vs `cross_plain_dav`.
