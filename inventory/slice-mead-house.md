# Slice 20 uniqueness claim — Mead House

Region id: `mead_house`. Hamlet pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, namehouse, peat fold, lens ruin, ropewalk, salt pans, smokehouse, eel weir, dye works, toll ferry, windpump, oyster park, counting house, ice cellar, wreck chapel, or bee skeps.

## Mechanic

Mash, bung, tap. Comb must is mashed, a bung is set, the cask is tapped. Not dye-vat color, not kiln grain-debt, not living-hive smoke.

## Inhabitants

- Keeper Hop (yard)
- Mal (mash)
- Sera (tap)

## Locations

`mead.path`, `mead.yard`, `mead.mash`, `mead.crock`, `mead.tap`. Linked from `ashfen.causeway` and `hive.path`.

## Outcome

`mead_drawn` — flag `mead_drawn`. Witness: `traces/marsh_mead_drawn.json`.

## Sheet / deed gates

Same scene `mead.yard`: marshborn/hunt `know_the_wild_must`; letters `read_the_cask_mark`.

## Cross-effect

Hive kept (`hive_kept`) unlocks `pitch_true_comb` at the mash. Proven by `cross_hive_mead` vs `cross_plain_mead`.
