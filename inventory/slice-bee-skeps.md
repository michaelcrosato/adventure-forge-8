# Slice 19 uniqueness claim — Bee Skeps

Region id: `bee_skeps`. Hamlet pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, namehouse, peat fold, lens ruin, ropewalk, salt pans, smokehouse, eel weir, dye works, toll ferry, windpump, oyster park, counting house, ice cellar, or wreck chapel.

## Mechanic

Smoke, comb, set. Light smoke calms the hive, comb is taken, the skep is set. Not fish-cure smoke, not kiln heat, not a wreck token rite.

## Inhabitants

- Keeper Sol (yard)
- Tansy (skeps)
- Wick (shed)

## Locations

`hive.path`, `hive.yard`, `hive.skeps`, `hive.comb`, `hive.shed`. Linked from `ashfen.causeway` and `wreck.path`.

## Outcome

`hive_kept` — flag `hive_kept`. Witness: `traces/marsh_hive_kept.json`.

## Sheet / deed gates

Same scene `hive.yard`: marshborn/hunt `know_the_hive_hum`; letters `read_the_skep_mark`.

## Cross-effect

Wreck laid (`wreck_laid`) unlocks `bless_the_skep` at the skeps. Proven by `cross_wreck_hive` vs `cross_plain_hive`.
