# Slice 6 uniqueness claim — Peat Fold

Region id: `peat_fold`. Hamlet pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, or namehouse.

## Mechanic

Peat share. A safe cut yields a brick that must go on the share board. Deep cuts flood the fold. No kiln heat-debt, no names-on-bone, no broth, no weather berm, no climb.

## Inhabitants

- Headwoman Brin (green)
- Cutter Jase (cut)
- Willa (shed)

## Locations

`fold.lane`, `fold.green`, `fold.cut`, `fold.shed`, `fold.ditch`. Linked from `ashfen.causeway` and `name.path`.

## Outcome

`fold_held` — flag `fold_held`. Witness: `traces/marsh_fold_held.json`.

## Sheet / deed gates

Same scene `fold.green`: marshborn/hunt `know_the_soft_cut`; letters `read_the_share_board`.

## Cross-effect

Name restored (`name_restored`) unlocks `cite_the_restored_name` on the green. Proven by `cross_name_fold` vs `cross_plain_fold`.
