# Slice 7 uniqueness claim — Lens Ruin

Region id: `lens_ruin`. Ruin pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, namehouse, or peat fold.

## Mechanic

Light path. A shard must be set so the low sun marks the channel. No peat share, no bone names, no broth, no kiln debt, no climb, no weather berm.

## Inhabitants

- Glazier Rook (nave)
- Nim (pit)
- Chart-keeper Lise (loft)

## Locations

`glass.path`, `glass.yard`, `glass.nave`, `glass.loft`, `glass.pit`. Linked from `ashfen.causeway` and `fold.lane`.

## Outcome

`lens_set` — flag `lens_set`. Witness: `traces/marsh_lens_set.json`.

## Sheet / deed gates

Same scene `glass.nave`: marshborn/hunt `know_the_low_sun`; letters `read_the_sun_chart`.

## Cross-effect

Fold held (`fold_held`) unlocks `trade_peat_for_lead` in the yard. Proven by `cross_fold_glass` vs `cross_plain_glass`.
