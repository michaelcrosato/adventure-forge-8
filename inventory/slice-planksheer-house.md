# Slice 131 uniqueness claim — Planksheer House

Region id: `planksheer_house`. Hamlet pocket. Not a reskin of gunwale snape-hance-dump, tabernacle score-mortise-pin, or skirting plane-base.

## Mechanic

Score, plane, dump. The sheer is scored, the plank is planed, and the edge is dumped. Not score_the_tabernacle, not plane_the_base, not dump_the_bolt.

## Inhabitants

- Sheer (yard)
- Plank (plank)
- Edge (edge)

## Locations

`psh.path`, `psh.yard`, `psh.sheer`, `psh.plank`, `psh.edge`. Linked from `ashfen.causeway` and `tab.path`. Gunwale owns “Go to the covering”; deadwood owns “Go to the rabbet”; sail has actor Shear.

## Outcome

`edge_dumped` — flag `edge_dumped`. Witness: `traces/marsh_edge_dumped.json`.

## Sheet / deed gates

Same scene `psh.yard`: marshborn/hunt `know_the_sheer`; letters `read_the_planksheer_list`.

## Cross-effect

Pinned keeper (`keeper_pinned`) unlocks `pin_the_sheer` at the sheer bench. Proven by `cross_tab_psh` vs `cross_plain_psh`.
