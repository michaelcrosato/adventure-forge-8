# Slice 132 uniqueness claim — Spirketting House

Region id: `spirketting_house`. Hamlet pocket. Not a reskin of planksheer score-plane-dump, clinker steam-clench-fair, or kevel chamfer-mortise-clench.

## Mechanic

Score, plane, clench. The spirket is scored, the strake is planed, and the spirket is clenched. Not score_the_sheer, not plane_the_plank, not clench_the_kevel.

## Inhabitants

- Spirket (yard)
- Strake (strake)
- Ring (ring)

## Locations

`spi.path`, `spi.yard`, `spi.spirket`, `spi.strake`, `spi.ring`. Linked from `ashfen.causeway` and `psh.path`. Clinker owns “Go to the steam” and “Go to the land”; kevel owns actor Clench.

## Outcome

`spirket_clenched` — flag `spirket_clenched`. Witness: `traces/marsh_spirket_clenched.json`.

## Sheet / deed gates

Same scene `spi.yard`: marshborn/hunt `know_the_spirket`; letters `read_the_spirketting_list`.

## Cross-effect

Dumped edge (`edge_dumped`) unlocks `dump_the_spirket` at the spirket bench. Proven by `cross_psh_spi` vs `cross_plain_spi`.
