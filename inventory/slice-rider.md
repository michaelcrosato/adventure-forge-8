# Slice 137 uniqueness claim — Rider House

Region id: `rider_house`. Hamlet pocket. Not a reskin of dagger-knee fair-side-mark, lodging-knee scribe-bolt-nick, or gunwale dump-bolt.

## Mechanic

Wring, seat, dump. The rider is wrung, the filling is seated, and the wring is dumped. Not wring as a rename of fair_the_dagger, not seat_the_gem, not dump_the_bolt.

## Inhabitants

- Rider (yard)
- Filling (filling)
- Wring (wring)

## Locations

`rid.path`, `rid.yard`, `rid.rider`, `rid.filling`, `rid.wring`. Linked from `ashfen.causeway` and `dag.path`. Path label is “Go to the riders” so yard “Go to the rider” is not co-legal. Gammon owns “Go to the riding”; thatch owns “Go to the stand” (so not Standard House).

## Outcome

`wring_dumped` — flag `wring_dumped`. Witness: `traces/marsh_wring_dumped.json`.

## Sheet / deed gates

Same scene `rid.yard`: marshborn/hunt `know_the_rider`; letters `read_the_rider_list`.

## Cross-effect

Marked sirmark (`sirmark_marked`) unlocks `mark_the_rider` at the rider bench. Proven by `cross_dag_rid` vs `cross_plain_rid`.
