# Slice 11 uniqueness claim — Eel Weir

Region id: `eel_weir`. Hamlet pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, namehouse, peat fold, lens ruin, ropewalk, salt pans, or smokehouse.

## Mechanic

Set and lift. A wicker basket is set on the stakes, then lifted with the catch. No smoke tend, no brine rake, no hemp twist, no road weather.

## Inhabitants

- Weaver Cess (yard)
- Noll (pool)
- Counter Meg (hut)

## Locations

`weir.path`, `weir.yard`, `weir.stakes`, `weir.pool`, `weir.hut`. Linked from `ashfen.causeway` and `smoke.path`.

## Outcome

`weir_lifted` — flag `weir_lifted`. Witness: `traces/marsh_weir_lifted.json`.

## Sheet / deed gates

Same scene `weir.yard`: marshborn/hunt `know_the_eel_run`; letters `read_the_weir_right`.

## Cross-effect

Smoke cured (`smoke_cured`) unlocks `bait_the_weir` at the stakes. Proven by `cross_smoke_weir` vs `cross_plain_weir`.
