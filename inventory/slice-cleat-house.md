# Slice 108 uniqueness claim — Cleat House

Region id: `cleat_house`. Hamlet pocket. Not a reskin of fid house, horn lantern, stay house, or deadwood bolt-hog.

## Mechanic

Saw, gouge, bolt. The cleat is sawn, the horn is gouged, and the base is bolted. Not fid turn-ream-seize, not scrape_the_horn, not bolt_the_hog.

## Inhabitants

- Saw (yard)
- Horn (horn)
- Bolt (bolt)

## Locations

`cleat.path`, `cleat.yard`, `cleat.saw`, `cleat.horn`, `cleat.bolt`. Linked from `ashfen.causeway` and `fid.path`.

## Outcome

`base_bolted` — flag `base_bolted`. Witness: `traces/marsh_base_bolted.json`.

## Sheet / deed gates

Same scene `cleat.yard`: marshborn/hunt `know_the_saw`; letters `read_the_cleat_list`.

## Cross-effect

Seized eye (`eye_seized`) unlocks `seize_the_saw` at the saw bench. Proven by `cross_fid_cleat` vs `cross_plain_cleat`.
