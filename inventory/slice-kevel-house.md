# Slice 127 uniqueness claim — Kevel House

Region id: `kevel_house`. Hamlet pocket. Not a reskin of cleat saw-gouge-bolt, deadwood dub, or futtock clench-belly.

## Mechanic

Chamfer, mortise, clench. The kevel is chamfered, the jaw is mortised, and the kevel is clenched. Not saw_the_cleat, not dub_the_timber, not clench_the_belly.

## Inhabitants

- Kevel (yard)
- Jaw (jaw)
- Clench (clench)

## Locations

`kev.path`, `kev.yard`, `kev.chamfer`, `kev.jaw`, `kev.clench`. Linked from `ashfen.causeway` and `chan.path`. Cleat owns “Go to the horn”; deadwood owns “Go to the dub”; fulling mill owns actor Stock.

## Outcome

`kevel_clenched` — flag `kevel_clenched`. Witness: `traces/marsh_kevel_clenched.json`.

## Sheet / deed gates

Same scene `kev.yard`: marshborn/hunt `know_the_kevel`; letters `read_the_kevel_list`.

## Cross-effect

Set strap (`strap_set`) unlocks `set_the_chamfer` at the chamfer bench. Proven by `cross_chan_kev` vs `cross_plain_kev`.
