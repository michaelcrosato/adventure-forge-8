# Slice 120 uniqueness claim — Crosstree House

Region id: `crosstree_house`. Hamlet pocket. Not a reskin of futtock cant-scarph-clench, deadeye seize-lanyard, or baluster rip-square.

## Mechanic

Square, spread, seize. The crosstree is squared, the hounds are spread, and the bolster is seized. Not cant_the_futtock, not seize_the_lanyard, not rip_the_square.

## Inhabitants

- Tree (yard)
- Hounds (hounds)
- Bolster (bolster)

## Locations

`cros.path`, `cros.yard`, `cros.tree`, `cros.hounds`, `cros.bolster`. Linked from `ashfen.causeway` and `futt.path`. Stay owns “Go to the arm”; crock owns “Go to the crocks”.

## Outcome

`bolster_seized` — flag `bolster_seized`. Witness: `traces/marsh_bolster_seized.json`.

## Sheet / deed gates

Same scene `cros.yard`: marshborn/hunt `know_the_tree`; letters `read_the_crosstree_list`.

## Cross-effect

Clenched belly (`belly_clenched`) unlocks `clench_the_tree` at the tree bench. Proven by `cross_futt_cros` vs `cross_plain_cros`.
