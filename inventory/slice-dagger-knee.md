# Slice 136 uniqueness claim — Dagger-knee House

Region id: `dagger_knee_house`. Hamlet pocket. Not a reskin of lodging-knee scribe-bolt-nick, stem hew-knee, or breast-hook score-bolt-fay.

## Mechanic

Fair, side, mark. The dagger is faired, the siding is sided, and the sirmark is marked. Not fair_the_garboard, not side as a rename of bolt_the_chock, not mark_the_nosing.

## Inhabitants

- Dagger (yard)
- Siding (siding)
- Sirmark (sirmark)

## Locations

`dag.path`, `dag.yard`, `dag.dagger`, `dag.siding`, `dag.sirmark`. Linked from `ashfen.causeway` and `lod.path`. Stem owns “Go to the knee” and actor Knee; lodging owns “Go to the lodging-knee”; thatch owns “Go to the stand” (so not Standard House).

## Outcome

`sirmark_marked` — flag `sirmark_marked`. Witness: `traces/marsh_sirmark_marked.json`.

## Sheet / deed gates

Same scene `dag.yard`: marshborn/hunt `know_the_dagger`; letters `read_the_dagger_list`.

## Cross-effect

Nicked lodging (`lodging_nicked`) unlocks `nick_the_dagger` at the dagger bench. Proven by `cross_lod_dag` vs `cross_plain_dag`.
