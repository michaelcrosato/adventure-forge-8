# Slice 128 uniqueness claim — Knighthead House

Region id: `knighthead_house`. Hamlet pocket. Not a reskin of kevel chamfer-mortise-clench, block cheek-strop, or stem hew-knee.

## Mechanic

Score, box, lash. The knight is scored, the bit is boxed, and the knight is lashed. Not score_the_channel, not lash_the_spar, not hoop_the_partner.

## Inhabitants

- Knight (yard)
- Box (box)
- Lash (lash)

## Locations

`kni.path`, `kni.yard`, `kni.knight`, `kni.box`, `kni.lash`. Linked from `ashfen.causeway` and `kev.path`. Block loft owns “Go to the cheek”; cooperage owns “Go to the hoop”; stem owns “Go to the stem”.

## Outcome

`knight_lashed` — flag `knight_lashed`. Witness: `traces/marsh_knight_lashed.json`.

## Sheet / deed gates

Same scene `kni.yard`: marshborn/hunt `know_the_knight`; letters `read_the_knighthead_list`.

## Cross-effect

Clenched kevel (`kevel_clenched`) unlocks `clench_the_knight` at the knight bench. Proven by `cross_kev_kni` vs `cross_plain_kni`.
