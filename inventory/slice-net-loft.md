# Slice 27 uniqueness claim — Net Loft

Region id: `net_loft`. Hamlet pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, namehouse, peat fold, lens ruin, ropewalk, salt pans, smokehouse, eel weir, dye works, toll ferry, windpump, oyster park, counting house, ice cellar, wreck chapel, bee skeps, mead house, cooperage, pickle house, tide forge, decoy pond, rushlight house, or caulk yard.

## Mechanic

Rig, mend, tar. Mesh is hung on the rails, the tear is mended, the net is tarred. Not ropewalk twist-and-walk, not weir baskets, not caulk oakum-and-pitch on a hull.

## Inhabitants

- Cobb (yard)
- Kip (rig)
- Darn (tar)

## Locations

`net.path`, `net.yard`, `net.rig`, `net.mesh`, `net.tar`. Linked from `ashfen.causeway` and `caulk.path`.

## Outcome

`net_tarred` — flag `net_tarred`. Witness: `traces/marsh_net_tarred.json`.

## Sheet / deed gates

Same scene `net.yard`: marshborn/hunt `know_the_mesh_hang`; letters `read_the_net_list`.

## Cross-effect

Seam caulked (`seam_caulked`) unlocks `pitch_the_net` at the tar pot. Proven by `cross_caulk_net` vs `cross_plain_net`.
