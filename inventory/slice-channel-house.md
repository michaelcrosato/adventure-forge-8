# Slice 126 uniqueness claim — Channel House

Region id: `channel_house`. Hamlet pocket. Not a reskin of boomkin steeve-bee-seize, deadeye score-reeve-seize, or hawse bolt-pay.

## Mechanic

Score, bolt, set. The channel is scored, the plate is bolted, and the strap is set. Not score_the_deadeye, not bolt_the_base, not set_the_roof.

## Inhabitants

- Channel (yard)
- Plate (plate)
- Strap (strap)

## Locations

`chan.path`, `chan.yard`, `chan.face`, `chan.plate`, `chan.strap`. Linked from `ashfen.causeway` and `boom.path`. Glazier owns “Go to the score”. `know_the_channel_cut` is a different suffix and is not co-legal with `know_the_channel`.

## Outcome

`strap_set` — flag `strap_set`. Witness: `traces/marsh_strap_set.json`.

## Sheet / deed gates

Same scene `chan.yard`: marshborn/hunt `know_the_channel`; letters `read_the_channel_list`.

## Cross-effect

Seized guy (`guy_seized`) unlocks `seize_the_face` at the face bench. Proven by `cross_boom_chan` vs `cross_plain_chan`.
