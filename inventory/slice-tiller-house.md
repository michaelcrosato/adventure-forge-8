# Slice 114 uniqueness claim — Tiller House

Region id: `tiller_house`. Hamlet pocket. Not a reskin of gammoning woold-ride-frap, bitts step-cross-belay, or windlass ship-pawl-heave.

## Mechanic

Shape, fit, yoke. The tiller is shaped, the helm is fitted, and the tiller is yoked. Not woold_the_bowsprit, not belay_the_cable, not ship_the_bars.

## Inhabitants

- Shape (yard)
- Helm (helm)
- Yoke (yoke)

## Locations

`till.path`, `till.yard`, `till.shape`, `till.helm`, `till.yoke`. Linked from `ashfen.causeway` and `gam.path`.

## Outcome

`tiller_yoked` — flag `tiller_yoked`. Witness: `traces/marsh_tiller_yoked.json`.

## Sheet / deed gates

Same scene `till.yard`: marshborn/hunt `know_the_shape`; letters `read_the_tiller_list`.

## Cross-effect

Frapped gammon (`gammon_frapped`) unlocks `frap_the_shape` at the shape bench. Proven by `cross_gam_till` vs `cross_plain_till`.
