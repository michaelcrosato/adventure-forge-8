# Slice 140 uniqueness claim — Pointer House

Region id: `pointer_house`. Hamlet pocket. Not a reskin of rider wring-seat-dump, futtock cant-scarph-clench, or crutch adze-fit-spike.

## Mechanic

Bevel, bed, dump. The pointer is bevelled, the inner is bedded, and the compound is dumped. Not bevel_the_station, not bed_the_sill, not dump_the_wring. I4 fingerprint var is `compounded` because `pointed` already names mason `joint_pointed`.

## Inhabitants

- Pointer (yard)
- Inner (inner)
- Compound (compound)

## Locations

`ptr.path`, `ptr.yard`, `ptr.diagonal`, `ptr.inner`, `ptr.compound`. Linked from `ashfen.causeway` and `cru.path`. Futtock owns “Go to the cant” and actor Cant; mould loft owns “Go to the bevel” internally (not co-legal). Rider owns filling/wring.

## Outcome

`compound_dumped` — flag `compound_dumped`. Witness: `traces/marsh_compound_dumped.json`.

## Sheet / deed gates

Same scene `ptr.yard`: marshborn/hunt `know_the_pointer`; letters `read_the_pointer_list`.

## Cross-effect

Spiked crotch (`crotch_spiked`) unlocks `spike_the_pointer` at the diagonal bench. Proven by `cross_cru_ptr` vs `cross_plain_ptr`.
