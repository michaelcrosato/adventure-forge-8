# Slice 135 uniqueness claim — Lodging-knee House

Region id: `lodging_knee_house`. Hamlet pocket. Not a reskin of breast-hook score-bolt-fay, stem hew-knee, or tile nick-arris.

## Mechanic

Scribe, bolt, nick. The lodging is scribed, the chock is bolted, and the lodging is nicked. Not scribe_the_wing, not bolt_the_palm, not nick_the_arris.

## Inhabitants

- Lodging (yard)
- Chock (chock)
- Nick (nick)

## Locations

`lod.path`, `lod.yard`, `lod.lodging`, `lod.chock`, `lod.nick`. Linked from `ashfen.causeway` and `brh.path`. Stem owns “Go to the knee” and actor Knee; tile owns “Go to the arris”.

## Outcome

`lodging_nicked` — flag `lodging_nicked`. Witness: `traces/marsh_lodging_nicked.json`.

## Sheet / deed gates

Same scene `lod.yard`: marshborn/hunt `know_the_lodging`; letters `read_the_lodging_list`.

## Cross-effect

Fayed hook (`hook_fayed`) unlocks `fay_the_lodging` at the lodging bench. Proven by `cross_brh_lod` vs `cross_plain_lod`.
