# Slice 113 uniqueness claim — Gammoning House

Region id: `gammon_house`. Hamlet pocket. Not a reskin of bitts step-cross-belay, parrel groove-thread-truss, or stem hew-gripe-hang.

## Mechanic

Woold, ride, frap. The bowsprit is woolded, the turns are ridden, and the gammon is frapped. Not belay_the_cable, not truss_the_parrel, not hang_the_knee.

## Inhabitants

- Woold (yard)
- Riding (riding)
- Frap (frap)

## Locations

`gam.path`, `gam.yard`, `gam.woold`, `gam.riding`, `gam.frap`. Linked from `ashfen.causeway` and `bitt.path`.

## Outcome

`gammon_frapped` — flag `gammon_frapped`. Witness: `traces/marsh_gammon_frapped.json`.

## Sheet / deed gates

Same scene `gam.yard`: marshborn/hunt `know_the_woold`; letters `read_the_gammon_list`.

## Cross-effect

Belayed cable (`cable_belayed`) unlocks `belay_the_woold` at the woold bench. Proven by `cross_bitt_gam` vs `cross_plain_gam`.
