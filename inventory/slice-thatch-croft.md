# Slice 38 uniqueness claim — Thatch Croft

Region id: `thatch_croft`. Hamlet pocket. Not a reskin of harbor, stacks, mill, court, road, fever camp, namehouse, peat fold, lens ruin, ropewalk, salt pans, smokehouse, eel weir, dye works, toll ferry, windpump, oyster park, counting house, ice cellar, wreck chapel, bee skeps, mead house, cooperage, pickle house, tide forge, decoy pond, rushlight house, caulk yard, net loft, sail loft, sounding stage, chart house, buoy yard, kelp shore, soap house, fulling mill, charcoal clamp, lime kiln, or mason yard.

## Mechanic

Cut, bind, set. Thatch is cut, bound into a yealm, and set on the ridge. Not reed-court law, not rushlight pith bind, not mason stone point.

## Inhabitants

- Rick (yard)
- Yealm (bind)
- Ridge (ridge)

## Locations

`thatch.path`, `thatch.yard`, `thatch.stand`, `thatch.yealm`, `thatch.ridge`. Linked from `ashfen.causeway` and `mason.path`.

## Outcome

`roof_set` — flag `roof_set`. Witness: `traces/marsh_roof_set.json`.

## Sheet / deed gates

Same scene `thatch.yard`: marshborn/hunt `know_the_yealm`; letters `read_the_thatch_list`.

## Cross-effect

Pointed joint (`joint_pointed`) unlocks `set_on_stone` at the ridge. Proven by `cross_mason_thatch` vs `cross_plain_thatch`.
