# Delegation

The orchestrator delegated a bounded builder task to grok-4.6-low (process default).

## Task

Implement the replay-verified player finding at Mira: marsh_friend must unlock a marsh-specific legal verb at `stacks.base`.

Acceptance:

- Authored action `share_marsh_path` in pack data (closed vocab).
- Finding session still replays.
- city_oath without marsh_friend does not get the verb.
- Prior outcome action sequences still reach `harbor_compact` and `stack_relic`.
- `verify` stays the bar; do not edit verify to pass.

## Integration

Orchestrator reviews the diff, rebuilds traces, runs `python -m adventure_forge verify`, and pushes if green.
