# Rejected report

Source: `rejected-report.json`

Claim: a player bought the harbor with `buy_the_harbor` and restored the compact.

Replay against the shipped engine rejects `buy_the_harbor` (not in the legal set). Failed mapping does not move the world. The report is discarded. It is not a verified finding.
